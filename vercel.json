{
    "functions": {
      "api/*.py": {
        "runtime": "python3.12",
        "memory": 1024,
        "maxDuration": 60
      }
    },
    "routes": [
      { "src": "/api/(.*)", "dest": "/api/$1", "methods": ["GET", "POST", "PUT", "DELETE", "PATCH"] }
    ]
  }